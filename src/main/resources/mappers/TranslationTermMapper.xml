<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hq.tech.nextpcb.web.dao.TranslationTermDao">

    <select id="selectByCategory" resultType="com.hq.tech.nextpcb.common.bean.entity.TranslationTerm">
        SELECT *
        FROM hq_translation_term
        WHERE delete_status = 0 and status = 1
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY weight DESC, id DESC
    </select>

    <select id="selectByCnLike" resultType="com.hq.tech.nextpcb.common.bean.entity.TranslationTerm">
        SELECT *
        FROM hq_translation_term
        WHERE delete_status = 0 and status = 1
        AND cn LIKE CONCAT('%', #{cn}, '%')
        ORDER BY weight DESC, id DESC
    </select>

    <select id="selectAllValid" resultType="com.hq.tech.nextpcb.common.bean.entity.TranslationTerm">
        SELECT *
        FROM hq_translation_term
        WHERE delete_status = 0 and status = 1
        ORDER BY weight DESC, id DESC
    </select>

    <update id="incrementUsageCount">
        UPDATE hq_translation_term
        SET usage_count = usage_count + 1, u_time = u_time
        WHERE id in
        <foreach collection="termIds" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </update>

</mapper>
